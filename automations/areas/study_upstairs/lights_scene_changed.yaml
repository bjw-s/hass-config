---
id: study_upstairs_lights_scene_changed
alias: Study Upstairs - Lights scene changed
max_exceeded: silent
trigger:
  - platform: state
    entity_id: input_select.study_upstairs_lights_scene
action:
  - service: scene.turn_on
    data:
      entity_id: scene.study_upstairs_lights_{{ trigger.to_state.state.replace(" ", "_") | lower }}
