---
platform: template
sensors:
  home_occupancy_home_status:
    friendly_name: 'Home presence'
    device_class: presence
    value_template: >
      {{ is_state('person.bernd', 'home')
          or is_state('person.man_yie', 'home')
          or is_state('input_boolean.home_modes_guest_mode', 'on')
      }}
