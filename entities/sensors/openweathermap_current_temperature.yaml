---
platform: template
sensors:
  openweather_current_temperature:
    value_template: |
      {% set current = state_attr('sensor.openweather_report', 'current') %}
      {{ current.temp }}
    unit_of_measurement: "°C"
    device_class: temperature
    