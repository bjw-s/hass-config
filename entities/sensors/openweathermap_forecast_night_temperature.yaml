---
platform: template
sensors:
  openweather_forecast_night_temperature:
    value_template: |
      {% set daily = state_attr('sensor.openweather_report', 'daily') %}
      {% if daily != None %}
        {{ daily[1].temp.min }}
      {% endif %}
    unit_of_measurement: "°C"
    device_class: temperature
