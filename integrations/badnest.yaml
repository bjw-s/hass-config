---
# A bad Nest integration that uses the web api to work after Works with Nest was shut down
#
# https://github.com/mattsch/badnest
#

badnest:
  issue_token: !env_var SECRET_NEST_ISSUE_TOKEN
  cookie: !env_var SECRET_NEST_COOKIE

climate:
  - platform: badnest
    scan_interval: 10
