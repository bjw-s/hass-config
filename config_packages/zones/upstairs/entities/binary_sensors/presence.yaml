---
platform: template
sensors:
  upstairs_presence:
    friendly_name: 'Upstairs - Presence'
    device_class: presence
    value_template: >-
      {%- set group = "group.home_room_presence_devices" -%}
      {%- set rooms = ["upstairs-office"] -%}
      {{- (expand(group) | selectattr("state","in",rooms) | list | count) > 0 -}}
